# 语言专项约定

这里收录修改对应语言代码时必须遵守的执行基线。只加载当前任务涉及的语言文件，不要为了读取
语言规则把 T0/T1 任务升级成架构评审。

## 路由

| 语言 | 约定 |
|---|---|
| Go | [[go]] |
| Python | [[python]] |
| TypeScript | [[typescript]] |

当前保持“一种语言一个文件”。当某个文件超过 reference 单文件预算，或形成多个需要独立演进的
主题时，再将它升级为同名目录，以 `README.md` 作为该语言入口；本路由只更新目标，不预建空目录。
